describe("changeviews", function () {
    describe("QuickInputView", function () {
        beforeEach(function () {
            models.days.reset();
        });
        afterEach(function () {
            models.days.reset();
        });
    });
});
