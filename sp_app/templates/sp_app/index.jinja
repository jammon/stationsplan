{% extends "base.jinja" %}
{% block content %}
{% include "sp_app/signup/page-header.jinja" %}

  <div class="row">
    <div class="col-md-4  pull-right">
      {% include "sp_app/login-form.jinja" %}
    </div>

    <div class="col-md-8 ">
{% macro pitch(heading, texts) -%}
        <div class="row">
          <div class="col-sm-4 col-xs-12 lead"><strong>{{ heading }}</strong></div>
          <div class="col-sm-8 col-xs-12 lead">
            {% for text in texts %}
              <div>{{ text }}</div>  
            {% endfor %}
          </div>
        </div>
{%- endmacro %}

    {{ pitch("Überall verfügbar",
    ['In der Klinik, zu Hause, unterwegs']) }}
    
    {{ pitch("Überblick behalten. ",
    ['Keine unbesetzten Positionen, keine "vergessenen" Personen']) }}
    
    {{ pitch("Gerechte Verteilung der Dienstbelastung.",
    ['Zählen Sie automatisch mit, wer wieviele Dienste bekommt.']) }}
    
    {{ pitch("Im eigenen Kalender. ",
    ['Importieren Sie Ihre Dienste in den Kalender ihres Smartphones oder Computers. Mit automatischen Updates!']) }}
    
    {{ pitch("In Ruhe planen, dann veröffentlichen.",
    ['Erst wenn der Dienstplan für den Monat fertig ist, geben Sie ihn zur Ansicht frei. ']) }}
    
    {{ pitch("Datensparsam.", 
    ["Kein Tracking, keine Werbung. Nur die wirklich notwendigen Daten werden erhoben. "]) }}
    
    {{ pitch("Geschützter Zugriff.", 
    ["Einblick in den Dienstplan nur mit Passwort."]) }}

    {{ pitch("Kostenlos und ohne Risiko testen.",
    ['Interessiert? Fangen Sie heute an mit der Stationsplanung auf moderne Art.',
     'Testen Sie in Ruhe. Wenn Ihre Kolleg/innen begeistert sind, bleiben Sie dabei.']) }}
    </div>
    <div class="col-md-4">
      <h2>Noch keinen Plan?</h2>
      <p class="lead">Sie können Stationsplan.de für Ihre Klinik 3 Monate lang kostenlos ausprobieren. Wenn das für Sie funktioniert - wunderbar. Wenn nicht, dann entstehen für Sie keine Verpflichtungen. </p>
      <p><a role="button" class="btn btn-primary btn-lg" href="/offer">Ausprobieren</a></p>
    </div>
  </div>
{% endblock content %}
{% block footer %}
  {% include "sp_app/footer.jinja" %}
{% endblock footer %}